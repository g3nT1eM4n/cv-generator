import React from 'react';
import { Input } from 'rsuite';

const MotivationInput: React.FC = () => (
  <Input
    componentClass="textarea"
    rows={4}
    placeholder="Motivation"/>
);

export default MotivationInput;
